#!/usr/bin/env node
require("./initializer.js").initializer;

var http = require('http');
// const { server } = require('./server/server.js');

// // TODO: Clear up all dependancies so this isn't needed!
// server.start();